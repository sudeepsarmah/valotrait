import IntroForm from "@/components/IntroForm"
import { useState } from "react"

export default function Questions() {
    const [isModalOpen, setIsModalOpen] = useState(true)

    return (

        <>
            <h1>Hi, take the test here</h1>
        </>
    )
}